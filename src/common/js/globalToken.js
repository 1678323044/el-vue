export default {
  userid_token: '',
  access_token: '',
  setUseridToken(userid_token){
    this.userid_token = userid_token
  },
  setAccessToken(access_token){
    this.access_token = access_token
  }
}

